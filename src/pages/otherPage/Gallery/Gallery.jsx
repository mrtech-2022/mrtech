import SectionTitle from '../../../components/SectionTitle/SectionTitle';

const Gallery = () => {
	return (
		<div>
			<SectionTitle title="Gallery" />
			<h1>After one year this section will come</h1>
		</div>
	);
};

export default Gallery;
